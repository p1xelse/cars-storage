---
cars-storage.router:
  advertise_uri: localhost:3301
  http_port: 8081

cars-storage.s1-master:
  advertise_uri: localhost:3302
  http_port: 8082

cars-storage.s1-replica:
  advertise_uri: localhost:3303
  http_port: 8083

cars-storage.s2-master:
  advertise_uri: localhost:3304
  http_port: 8084

cars-storage.s2-replica:
  advertise_uri: localhost:3305
  http_port: 8085

cars-storage-stateboard:
  listen: localhost:3310
  password: passwd
